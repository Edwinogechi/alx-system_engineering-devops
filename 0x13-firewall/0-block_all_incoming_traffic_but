# Checking the status of ufw:

sudo ufw status

# Enable ufw if not active:
suo ufw enable

# Set the default incoming policy to "deny" all incoming traffic:
sudo ufw default deny incomming

# Set the default outgoing policy to allow  all outgoing traffic by running:
sudo ufw default allow outgoing

# Allow incoming traffic on the specified TCP ports by running:

sudo ufw allow 22/tcp
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp

# Verifyif the given rules have been added:

sudo ufw show added

# Enable ufw by running it finally:
sudo ufw enable
